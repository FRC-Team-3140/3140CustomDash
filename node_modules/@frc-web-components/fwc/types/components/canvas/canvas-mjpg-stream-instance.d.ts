import { LitElement } from 'lit';
import { CanvasObjectApi } from './interfaces';
export default class CanvasMjpgStreamInstance extends LitElement {
    src: string;
    width: number | null;
    height: number | null;
    origin: [number, number];
    disabled: boolean;
    hideCrosshair: boolean;
    crosshairColor: string;
    private image;
    private connected;
    private loadedTimeout?;
    private throttleUpdateImage;
    private onImageLoadBound;
    private onImageErrorBound;
    static styles: import('lit').CSSResult;
    private getImageSize;
    protected draw({ ctx, canvas }: CanvasObjectApi): void;
    disconnectedCallback(): void;
    connectedCallback(): void;
    private isImageLoaded;
    private updateImage;
    private setConnected;
    private onImageLoad;
    private onImageError;
    private dispatchConnect;
    private dispatchDisconnect;
    protected updated(changedProps: Map<string, unknown>): void;
}
declare global {
    interface HTMLElementTagNameMap {
        'frc-canvas-mjpg-stream-instance': CanvasMjpgStreamInstance;
    }
}
